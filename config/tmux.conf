# tmux server dedicated to the Claude sessions served in the browser.
set -g status off
set -g mouse on
set -g history-limit 50000
set -g default-terminal "xterm-256color"
set -g focus-events on
set -g destroy-unattached off
